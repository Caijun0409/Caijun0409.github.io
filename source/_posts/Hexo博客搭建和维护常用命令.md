---
title: Hexo博客搭建与维护常用命令速查
date: 2025-11-19 18:50:00
tags:
  - Hexo
  - Git
  - 备忘录
categories:
  - 技术学习
---

在使用 Hexo + Redefine 主题搭建博客的过程中，遇到了一些环境依赖和部署的问题。为了防止以后忘记，特将常用的核心命令和工作流整理如下。

## 1. 日常写作与发布 (核心)

这是写完文章后，将网站发布给别人看的流程。

| 简写命令        | 完整命令          | 说明                                                      |
| :-------------- | :---------------- | :-------------------------------------------------------- |
| `hexo n "标题"` | `hexo new "标题"` | 新建一篇文章                                              |
| `hexo cl`       | `hexo clean`      | 清理缓存 (清除 public 和 db.json)，**推荐每次部署前执行** |
| `hexo g`        | `hexo generate`   | 生成静态网页 (编译源码到 public 目录)                     |
| `hexo s`        | `hexo server`     | 本地预览，访问 `http://localhost:4000`                    |
| `hexo d`        | `hexo deploy`     | 部署到 GitHub Pages (main 分支)                           |

**🚀 推荐的一键部署“连招”：**

```bash
hexo clean && hexo g && hexo d
```

##  2.源码备份（Git工作流）

`hexo deploy`只上传网页，不上传源码，为了防止换电脑后博客丢失，必须手动备份源码到Github的source分支

常用的备份命令如下：
```
#1.查看当前状态
git status

# 2.添加所有修改到暂存区
git add .

# 3.提交修改说明
git commit -m "更新博客内内容：添加了新文章"

# 4.推送到远程source分支
git push origin source
```

## 3.环境与插件安装

由于 Node.js 版本较高 (v7+)，安装 Hexo 插件时经常遇到 `ERESOLVE` 依赖冲突报错。

**解决方法：强制忽略对等依赖检查**

```
# 安装所有依赖（初次拉取源码后）
npm install --legacy-peer-deps

# 安装特定插件（例如 git 部署插件）
npm install hexo-deployer-git --save --legacy-peer-deps
```

## 4.SSH连接配置

ssh密钥生成与验证

```
# 1. 生成 ED25519 密钥 (推荐一路回车，不设密码)
ssh-keygen -t ed25519 -C "你的GitHub邮箱@example.com"

# 2. 查看公钥 (复制内容到 GitHub -> Settings -> SSH keys)
cat ~/.ssh/id_ed25519.pub(window一般在C盘里)

# 3. 验证连接
ssh -T git@github.com
# 成功会显示: You've successfully authenticated
```

## 5.其他实用命令

```
# 本地预览草稿
hexo server --draft(可以查看source/_drafts下的文章)

# 列出文章
hexo list post

# 查看Hexo版本
hexo version
```

